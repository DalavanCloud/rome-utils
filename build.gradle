apply plugin: 'java'
apply plugin: 'maven'

if(project.hasProperty('standalone')) {
    group = 'name.kazennikov'
    description = "rome"
    version = '0.0.1-SNAPSHOT'

}


repositories {

    mavenCentral()
}